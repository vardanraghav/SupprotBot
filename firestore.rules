rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to all data for this demo.
    // In a real application, you would secure this based on your app's needs.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
